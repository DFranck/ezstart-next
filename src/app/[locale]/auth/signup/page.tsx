import Section from "@/components/layout/section";
import SignUpForm from "@/providers/auth/sign-up-form";

const page = () => {
  return (
    <Section>
      <SignUpForm />
    </Section>
  );
};

export default page;
