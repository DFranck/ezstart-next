import ComingSoon from "@/components/layout/coming-soon.";
import { Section } from "lucide-react";

const page = () => {
  return (
    <Section>
      <ComingSoon message="Admin documentation is under construction. Stay tuned for updates!" />
    </Section>
  );
};

export default page;
